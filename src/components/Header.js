//rsf tab to get the functional component skeleton

import React from 'react';

function Header(props) {
    return (
        <h1>
            Hello from Header
        </h1>
    );
}

export default Header;